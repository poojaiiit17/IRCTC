CREATE DATABASE IF NOT EXISTS railway_db;
USE railway_db;

-- Spring Boot/JPA creates and updates the tables automatically.
-- Run this file first, then set your MySQL password in:
-- backend/src/main/resources/application.properties
